@model IEnumerable<WebAppPP3.Models.NumeroMaxPuestos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CantidadPuestos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CantidadPuestos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
         

        </td>
    </tr>
}

</table>
